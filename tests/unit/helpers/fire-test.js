
import { fire } from 'q-and-a/helpers/fire';
import { module, test } from 'qunit';

module('Unit | Helper | fire');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fire([42]);
  assert.ok(result);
});

